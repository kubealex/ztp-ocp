apiVersion: v1
kind: Secret
metadata:
  name: {{ ztp_cluster_name | default('ztp-sno', true) }}-pull-secret
  namespace:
data:
  .dockerconfigjson: {{ ztp_pull_secret | b64encode }}
type: kubernetes.io/dockerconfigjson
