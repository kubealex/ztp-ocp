apiVersion: v1
kind: Secret
metadata:
  name: {{ ztp_cluster_name | default('ztp-sno', true) }}-bmc-credentials
data:
  password: {{ ztp_bmc_password | b64encode }}
  username: {{ ztp_bmc_username | b64encode }}
